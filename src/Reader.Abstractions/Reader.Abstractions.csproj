<Project Sdk="Microsoft.NET.Sdk">

  <PropertyGroup>
    <TargetFramework>net8.0</TargetFramework>
    <RootNamespace>OpenReader.Abstractions</RootNamespace>
    <AssemblyName>OpenReader.Abstractions</AssemblyName>
    <Description>Platform-neutral interfaces and value types. The contract every other project depends on.</Description>
  </PropertyGroup>

  <!-- NuGet metadata: this project is shipped to plugin authors as `OpenReader.Sdk`.
       The assembly name remains `OpenReader.Abstractions` so the host and plugins
       resolve to the same loaded type identity. -->
  <PropertyGroup>
    <IsPackable>true</IsPackable>
    <PackageId>OpenReader.Sdk</PackageId>
    <Title>OpenReader Plugin SDK</Title>
    <PackageDescription>The contract for OpenReader app modules. Reference this package when writing a plugin: implement IAppModule, ship a manifest.json, and OpenReader will load you.</PackageDescription>
    <PackageTags>openreader;screen-reader;accessibility;plugin;sdk</PackageTags>
    <PackageProjectUrl>https://github.com/OpenReader/OpenReader</PackageProjectUrl>
    <RepositoryUrl>https://github.com/OpenReader/OpenReader</RepositoryUrl>
    <RepositoryType>git</RepositoryType>
    <PackageLicenseExpression>MIT</PackageLicenseExpression>
    <PackageReadmeFile>README.md</PackageReadmeFile>
    <Version Condition="'$(Version)' == ''">0.1.0</Version>
    <PackageReleaseNotes>Initial preview. API version 1.0. Contract surface: IAppModule, IAppContext, AppModuleManifest, ProcessInfo, IAccessibilityProvider, AccessibleNode, AccessibilityEvent, ISpeechEngine, SpeechRule, SpeechUtterance, IInputSource, RawInput.</PackageReleaseNotes>
    <IncludeSymbols>true</IncludeSymbols>
    <SymbolPackageFormat>snupkg</SymbolPackageFormat>
  </PropertyGroup>

  <ItemGroup>
    <None Include="README.md" Pack="true" PackagePath="\" />
  </ItemGroup>

</Project>
